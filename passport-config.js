const localStrategy = require('passport-local').Strategy

function initializePassport(passport){
    const authenticateUser = (email, password, done) => { // needs to interact with user model

    }

}